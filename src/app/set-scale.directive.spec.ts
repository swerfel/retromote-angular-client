import { SetScaleDirective } from './set-scale.directive';

describe('SetScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new SetScaleDirective();
    expect(directive).toBeTruthy();
  });
});
